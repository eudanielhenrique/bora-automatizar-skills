#!/bin/bash
set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR"

echo ""
echo "  bora-automatizar-skills — update"
echo "  ----------------------------------"
echo ""

git pull

./setup
